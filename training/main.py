import logging
import os
import subprocess
from pyfasttext import FastText
import glob

labelFields=[]
fields = []
filename = ""

#we get a filename, fields to get training data from
#and fields containing labels for supervised learning

/models
/jsonfiles

models = "/data/models/"
jsonfiles = glob.glob("/data/jsonfiles/*.json")

f=file.open(f"{jsonfile!s})

def readJson(self):
    print("read file")